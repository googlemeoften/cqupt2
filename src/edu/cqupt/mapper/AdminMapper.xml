<?xml version="1.0" encoding="UTF-8"?>  
<!DOCTYPE mapper  
	PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"  
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="edu.cqupt.mapper.AdminMapper">
	<select id="findAllAdmin" resultType="admin">
		SELECT * FROM admin
	</select>
	
	<select id="findAdminByName" parameterType="string" resultType="admin">
		SELECT * FROM admin WHERE admin_name=#{admin_name}
	</select>
	
	<insert id="addAdmin" parameterType="admin">
		INSERT INTO admin (admin_name,PASSWORD,contact,LEVEL)VALUE(#{admin_name},#{PASSWORD},#{contact},#{LEVEL})
	</insert>
	
	<delete id="deleteAdmin" parameterType="int">
		DELETE FROM admin WHERE admin_id=#{admin_id}
	</delete>
</mapper>